# Ctrl+F switches to vim command mode
# This allows you to use vim commands on your current shell line
bindkey "^F" vi-cmd-mode

# Ctrl+A move to beginning on the shell line
bindkey "^A" beginning-of-line

# Ctrl+E to move to end of the shell line
bindkey "^E" end-of-line